[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fashion-mm"
version = "0.1.0"
description = "Fine-grained fashion multimodal understanding modules."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
